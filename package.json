{
  "name": "irctc-async",
  "version": "1.0.0",
  "private": true,
  "description": "IRCTC-style prepare-ahead Tatkal booking demo — client-only (localStorage), no backend.",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client install && npm --prefix client run build",
    "preview": "npm --prefix client run preview"
  }
}
